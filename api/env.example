# ============================================
# CONFIGURAÇÃO DO BANCO DE DADOS MYSQL (WordPress Docker)
# ============================================
# IMPORTANTE: NUNCA commite este arquivo com senhas reais!
# Use este arquivo como template e crie um .env com valores reais
#
# Para WordPress rodando no Docker:
# - WP_DB_HOST deve ser o nome do container MySQL (ex: wp_db)
# - Ou use o IP do container na rede Docker
# - Se a API estiver na mesma rede Docker, use o nome do container

# Host do MySQL (nome do container Docker ou IP)
WP_DB_HOST=wp_db

# Credenciais do MySQL (do container wp_db)
WP_DB_USER=wp_user
WP_DB_PASSWORD=WpUser@2024!Strong#Pass
WP_DB_NAME=wordpress_db
WP_TABLE_PREFIX=wp_

# SSL para conexão MySQL (true/false)
WP_DB_SSL=false

# ============================================
# URL BASE DO WORDPRESS
# ============================================
# URL base do WordPress (para construir URLs completas de imagens)
WP_URL=https://phdstudio.com.br

# ============================================
# SEGURANÇA - API KEY
# ============================================
# IMPORTANTE: Gere uma chave segura usando:
# openssl rand -base64 32 | tr -d "=+/" | cut -c1-32
# NUNCA use a chave padrão em produção!
PHD_API_KEY=GERE_UMA_CHAVE_SEGURA_AQUI

# ============================================
# CORS - ORIGENS PERMITIDAS
# ============================================
# Separe múltiplas origens por vírgula
# Exemplo: https://phdstudio.com.br,https://app.phdstudio.com.br
ALLOWED_ORIGINS=*

# ============================================
# CONFIGURAÇÃO DO SERVIDOR
# ============================================
# Porta da API
API_PORT=3001

# Ambiente (development/production)
NODE_ENV=production
